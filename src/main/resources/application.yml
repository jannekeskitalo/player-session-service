spring:
  profiles:
    active: local

---
spring:
  profiles: local
  data:
    cassandra:
      keyspace-name: test
      schema-action: recreate
      contact-points: localhost

---
spring:
  profiles: docker
  data:
    cassandra:
      keyspace-name: test
      schema-action: recreate
      contact-points: cassandra
logging:
  level:
    com.datastax.driver: DEBUG

